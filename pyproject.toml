[project]
name = "online-finetune-platform"
version = "0.1.0"
requires-python = ">=3.11"

[tool.uv.workspace]
members = [
  "apps/api",
  "apps/embedding",
  "apps/worker",
  "libs/python-sdk",
  "libs/protos",
]
