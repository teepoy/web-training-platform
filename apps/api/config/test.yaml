app:
  env: test
  frontend_url: "http://localhost:5173"

execution:
  engine: local

logging:
  level: WARNING

db:
  url: "sqlite+aiosqlite:///./finetune-test.db"
  echo: false
  auto_create: true

storage:
  kind: memory

notification:
  webhook:
    endpoint: "http://localhost:9000/hooks/training"

label_studio:
  url: "http://localhost:8080"
  external_url: "http://localhost:8080"

auth:
  jwt_secret_key: "test-only-jwt-signing-key"

oauth:
  enabled: false

agent:
  enabled: true
  metadata_sample_size: 50
