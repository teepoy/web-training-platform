[project]
name = "ftsdk"
version = "0.1.0"
description = "Finetune platform Python SDK"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "platform-runtime",
]

[project.scripts]
ftctl = "ftsdk.cli:app"

[tool.uv.sources]
platform-runtime = { workspace = true }
