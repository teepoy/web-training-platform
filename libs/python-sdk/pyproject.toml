[project]
name = "ftsdk"
version = "0.1.0"
description = "Finetune platform Python SDK"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "httpx>=0.28.0",
  "typer>=0.16.0",
]

[project.scripts]
ftctl = "ftsdk.cli:app"
