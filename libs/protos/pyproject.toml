[project]
name = "embedding-protos"
version = "0.1.0"
description = "Generated gRPC stubs for embedding service"
requires-python = ">=3.11"
dependencies = [
  "grpcio>=1.68.0",
  "protobuf>=5.29.0",
]

[project.optional-dependencies]
codegen = [
  "grpcio-tools>=1.68.0",
]
